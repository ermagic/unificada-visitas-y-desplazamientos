# Nuevo database.py para Google Sheetsimport streamlit as stfrom streamlit_gsheets import GSheetsConnectionimport pandas as pd# Establece la conexión usando los secretosconn = st.connection("gcs", type=GSheetsConnection)def obtener_usuarios():    return conn.read(worksheet="usuarios")def obtener_visitas():    return conn.read(worksheet="visitas")def insertar_usuario(datos_nuevos_df):    # Esta función es un ejemplo, habría que adaptarla    # La librería permite actualizar y añadir filas    # Por ejemplo, para añadir una fila:    # conn.update(worksheet="usuarios", data=datos_nuevos_df)    pass # Aquí iría la lógica para añadir/actualizar filas